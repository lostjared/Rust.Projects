Simple Example to process an (PNG 8 bit RGBA) image in Rust from the command line:

Required Arguments:
	--index <index>
	--depth <depth>
	--output <output>
	<FILE>
or
    --list 

USAGE:
    filter [OPTIONS] --index <index> --depth <depth> --output <output> <FILE>

